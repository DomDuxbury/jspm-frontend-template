import React from 'react';
import ReactDOM from 'react-dom';
import HomePage from '../containers/homePage.js';
import Bootstrap from 'bootstrap';

let appNode = document.getElementById('app');
ReactDOM.render(<HomePage/>, appNode);
